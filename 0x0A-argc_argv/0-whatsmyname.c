#include <stdio.h>

/**
 * main - entry level
 *
 * @argc: argument count
 * @argv: argument array
 *
 * Return: always 0 success
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

